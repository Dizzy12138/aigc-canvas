# Example environment file for xingliu-canvas-v2

# Port for the Express server
PORT=4000

# MongoDB connection string (update as needed)
MONGO_URI=mongodb://127.0.0.1:27017/aigc_canvas

# JWT secret for authentication (change in production)
JWT_SECRET=change_this_secret

# Base URL for a ComfyUI API (optional). If set, AI calls will be forwarded
# to this URL; if not set the server will simulate responses with
# placeholder images.
COMFYUI_API_URL=http://127.0.0.1:8188